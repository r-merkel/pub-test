---
name: reusable workflow
on:
  workflow_call:
    inputs:
      test:
        required: true
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest
    needs: Build
    steps:
      - name: get artifact
        uses: actions/download-artifact@v2
        with:
          name: test
    
